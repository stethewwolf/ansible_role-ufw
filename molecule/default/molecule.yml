---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: instance
    image: "docker.io/library/debian:stable"
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
verifier:
  name: ansible
